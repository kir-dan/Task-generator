%%%TEMPLATE

Решите уравнение
$$\frac{\sin %a x}{\sin %b x} - \frac{\cos %a x}{\cos %b x} =
\frac{\sin %b x}{\sin %a x} - \frac{\cos %b x}{\cos %a x}$$

%%%ATTRIBUTE

%%%SOLVE

(= b (generateint 1 6))
(= a (generateint 1 6 '(!= x b)))
(= ans1 (abs (* 2 (- a b))))
(= ans2 (abs (+ a b)))
(= no_ans1 (abs (* 2 a)))
(= no_ans2 (abs (* 2 b)))

%%%ANSWER
$$x = \frac{\pi n}{%ans1}, \frac{\pi m}{%ans2}; x \neq \frac{\pi k}{%no_ans1},
\frac{\pi l}{%no_ans2}$$